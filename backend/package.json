{
  "name": "ai-dungeon-master-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "db:init": "node -e \"import('./src/config/db.js').then(m => m.initDB()).then(() => console.log('DB initialized'))\"",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "dnd-data": "^1.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
